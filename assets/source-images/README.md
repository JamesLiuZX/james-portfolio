# Source images

Full-resolution originals. They are deliberately outside `public/` so they are
not copied into the static export — `public/*.webp` holds the resized,
re-encoded versions the site actually serves.

Regenerate the WebP files from these if you ever need different dimensions.
